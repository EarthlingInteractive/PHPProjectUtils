{
	"name": "earthit/php-project-utils",
	"description": "Build utilities for PHP projects, in PHP.",
	"license": "MIT",
	"require": { },
	"suggest": {
		"togos/base32": "Allows storing upgrade scripts into a ccouch-style blobstore."
	},
	"bin": [
		"bin/generate-psql-script",
		"bin/upgrade-database",
		"bin/generate-create-database-sql",
		"bin/generate-drop-database-sql"
	],
	"autoload": {
		"psr-0": {
			"EarthIT_ProjectUtil_": "lib/"
		}
	}
}
